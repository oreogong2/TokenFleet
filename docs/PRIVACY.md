# TokenFleet 隐私说明

TokenFleet 首版是本地优先、由管理员邀请接入的社群 AI Token 用量工具。
没有会员体系、参赛者登录、微信登录、开放注册或面向所有人的自助接入。管理员可以
生成单批最多 50 人、最长 90 天且可随时关闭的受限批次链接（默认 24 小时，90 天需显式选择）；同一社群可创建多个批次，持有该链接的受邀成员只
填写唯一昵称并明确同意公开。后台只为参赛者保存昵称和内部归属，不会为了接入伪造
邮箱或共享密码。

## 本地读取什么

官方客户端只从已支持工具的固定本地日志或只读数据库中提取：

- 日期或时间戳；
- 客户端、模型和采集来源；
- 输入、输出、缓存读、缓存写 Token 数量；
- 采集器与 schema 版本。

macOS 首版可靠支持 Codex、Claude Code 和 CC Switch；ZCode 作为默认关闭的实验来源，
只在成员主动开启后读取独立 SQLite `model_usage` 用量表。Windows 支持 Codex、Claude Code，
并在 ZCode 用量库存在时自动只读同一张 `model_usage` 表。两端均不读取 ZCode 会话 transcript、
Coding Plan 凭证或剩余额度；Windows 暂不采集 CC Switch。客户端只读取各平台已声明的固定数据位置，不要求
管理员权限，不注入其他进程，不抓网络流量，也不安装根证书。

macOS 还保留了两项与本地用量展示有关的外部集成，不属于社群同步：

- “显示 Codex / Claude 额度”默认关闭。用户主动开启后，TokenFleet 会启动本机
  `codex app-server`子进程读取 Codex 额度；
- 同一额度开关开启后，TokenFleet 会通过系统 `/usr/bin/security` 只读查找
  Claude Code 的 `Claude Code-credentials` 钥匙串项，仅为取得 OAuth access token
  并请求 `https://api.anthropic.com/api/oauth/usage` 显示 Claude 额度。该 token
  不会写入 TokenFleet 设置、日志或社群上报，也不会发送给 TokenFleet
  服务器。

这些功能不会改变下文的社群上传字段；社群同步不上传 Claude / Codex 登录凭证。

## 何时上传

默认本地使用不上传。受邀成员打开管理员生成的批次链接后，先填写唯一公开昵称并
明确同意公开字段；服务端在同一事务中创建非登录参赛者和个人 60 分钟单次设备码。
批次令牌从 URL fragment 读入闭包内存，个人设备码只存在于 claim 响应和闭包内存，
正常情况下都不会发送进访问日志；页面不得把它们写进 Cookie、浏览器存储、DOM、日志、分析事件或截图，读取后
会立即从地址栏/历史中移除，也不会自动读取剪贴板或唤起客户端。对已有参赛者的第二
台设备，管理员仍生成单独设备链接。官方安装命令把唯一社群 HTTPS 地址写进客户端配置，运行时不
提供成员覆盖入口；地址由安装/构建产物固定，不是通用源码里的硬编码域名。成员只
粘贴连接码，不能改成任意服务器。确认连接会立即上传当前仍可验证的历史日聚合，并在之后持续后台
同步；连接码使用一次即失效。成功登记后，每台设备使用独立凭据同步。关闭连接不
删除本地 Token 数据。

官方客户端只上传按日、工具、模型聚合的四类 Token 和最小版本元数据。服务端
schema 不接收 prompt、回复、代码、文件、路径、项目、repo、skill、MCP、agent、
会话正文、Claude/Codex 登录凭证或任何第三方服务凭证。

## 社群榜公开什么

现有成员默认不公开。管理员获得成员同意并开启“参与社群榜”后，公开页和分享图
可以展示：

- 公开昵称与随机公开 ID；
- 所选时间范围的排名；
- 输入、输出、缓存读、缓存写和总 Token；
- 工具、模型、按日趋势；
- 按已配置价格表计算的估算费用。

公开接口和分享图不展示：

- 邮箱、密码、微信身份或内部用户 ID；
- 设备 ID、设备数量/明细、IP 或服务器内部信息；
- 逐小时作息、会话数、消息数或精确同步时间；
- 城市、文件/项目路径、prompt、代码或对话内容；
- enrollment token、device secret、HMAC 或登录令牌。

成员被禁用或关闭公开后会立即从公开榜和公开个人页消失，私有账本按保留策略
继续保存，便于管理员核账、导出或删除。

## 费用含义

公开费用是按 Token 与版本化价格表计算的 API 等价估算，不是实际账单。匹配不到
可靠价格时显示“未定价”，不会显示为零，也不会套模型家族兜底价。固定订阅、赠送
额度、协议折扣和供应商发票不从 Token 反推。既有价格默认视为私有；只有管理员
明确标记为“用于社群榜公开估算”的价格版本才会贡献公开费用。

## 本地文件与凭据

TokenFleet 本地数据位于：

```text
~/Library/Application Support/TokenFleet
```

普通设置文件只保存非敏感连接状态和聚合数据。macOS 的社群设备 secret 只保存在
TokenFleet 自己的 Keychain 项；这条社群凭据存储链路不读取、修改或删除其他 App
的钥匙串项。上文明确披露的 Claude 额度功能是另一条默认关闭的只读链路。
Windows 的设备 secret 必须用当前用户 DPAPI 加密且不得进入 roaming 设置；
两端都不允许把社群 secret 降级到明文文件、偏好设置、日志或截图。安全凭据
能力不可用时，社群同步保持关闭。

## 分享与外部排行榜

分享 PNG 在浏览器本地从公开数据生成。二维码和链接只指向无凭证的同源 HTTPS
公开榜，不调用第三方截图或二维码服务。

第三方排行榜或社区服务仍由成员自行连接；TokenFleet 不读取、代理或保存其
专属上传地址、token 或 device secret。

以上边界可以验证官方客户端和官方服务端的协议形状，但不能证明被恶意修改的
第三方客户端不会把内容编码进合法标签。设备邀请、独立签名密钥、限流、异常复核、
禁用与可撤销公开共同构成信任边界。
